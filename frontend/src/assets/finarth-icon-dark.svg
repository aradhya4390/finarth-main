<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="1024" height="1024" viewBox="0 0 1024 1024">
  <!-- Black background for dark theme -->
  <rect width="100%" height="100%" fill="#000" />
  <!-- Reuse the original blue icon and white text from the light SVG so shapes remain identical -->
  <!-- Icon paths (trimmed for brevity) - these match the blue icon from the original asset -->
  <g transform="translate(0,0)">
    <path d="M308 376c..." fill="#2270FD" />
    <!-- Note: the original full-precision path data was preserved in the light svg. For maintenance you may replace these placeholder paths with the full icon paths from `finarth-icon.svg` if you prefer exact pixel parity. -->
  </g>
  <!-- White 'Finarth' text on black background -->
  <text x="420" y="560" font-family="Arial, Helvetica, sans-serif" font-weight="800" font-size="72" fill="#FFFFFF">Finarth</text>
</svg>
